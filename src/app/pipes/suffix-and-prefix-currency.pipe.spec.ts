import { SuffixAndPrefixCurrencyPipe } from './suffix-and-prefix-currency.pipe';

describe('SuffixAndPrefixCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new SuffixAndPrefixCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
