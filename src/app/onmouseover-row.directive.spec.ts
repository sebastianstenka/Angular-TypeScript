import { OnmouseoverRowDirective } from './onmouseover-row.directive';

describe('OnmouseoverRowDirective', () => {
  it('should create an instance', () => {
    const directive = new OnmouseoverRowDirective();
    expect(directive).toBeTruthy();
  });
});
