import { OnmouseoverImgDirective } from './onmouseover-img.directive';

describe('OnmouseoverImgDirective', () => {
  it('should create an instance', () => {
    const directive = new OnmouseoverImgDirective();
    expect(directive).toBeTruthy();
  });
});
